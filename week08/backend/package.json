{
  "name": "product-api",
  "version": "1.0.0",
  "description": "No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)",
  "main": "index.js",
  "scripts": {
	"prestart": "npm install",
	"start": "node index.js",
	"test:api": "start-server-and-test start http://localhost:8080/docs \"newman run ../postman/collection.json -e ../postman/env.json -r cli,json --reporter-json-export ../postman/reports/report.json\"",
	"test:api:once": "newman run ../postman/collection.json -e ../postman/env.json -r cli,json --reporter-json-export ../postman/reports/report.json"
  },
  "keywords": [
    "swagger"
  ],
  "license": "Unlicense",
  "private": true,
  "dependencies": {
    "connect": "^3.2.0",
    "js-yaml": "^3.3.0",
    "oas3-tools": "^2.2.3"
  },
  "devDependencies": {
    "newman": "^6.2.0",
    "start-server-and-test": "^2.0.2"
  }
}
